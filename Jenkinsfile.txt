pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                echo 'Deploying to Cloud...'
                sh 'docker compose up -d'
            }
        }
    }
}